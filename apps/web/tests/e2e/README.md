# Archived E2E Tree

This directory is intentionally inactive.

The live Playwright suite runs from [apps/web/e2e](/home/amd/dev/clients/uni-salzburg/qdesigner-modern/apps/web/e2e) via [apps/web/playwright.config.ts](/home/amd/dev/clients/uni-salzburg/qdesigner-modern/apps/web/playwright.config.ts).

Do not add executable end-to-end specs under `apps/web/tests/e2e`. Keep browser coverage in `apps/web/e2e` so selectors, helpers, and CI configuration have a single source of truth.
