# Follow-ups discovered during implementation

Items surfaced while implementing/reviewing units that are **out of the current unit's scope**
but should be tracked. Not defects introduced by the work — either pre-existing gaps or
deliberate scope boundaries.

| # | From | Item | Suggested home |
|---|---|---|---|
| F-1 | P2-T2 | Main-thread `ScriptExecutor` hooks still defeatable by string-concat obfuscation (`x['con'+'structor']`) and have no hard timeout. Full fix = move hooks onto the Web Worker with an **async hook contract** (QuestionnaireRuntime executeOnMount/Validate/Response/Navigate are synchronous today). | New runtime epic (relates to E-FLOW / reaction async work) |
| F-2 | P2-T9 | `openapi.json` carries accumulated pre-existing drift (missing `change_member_role`, media streaming proxy, `public_cohort_stats`, `CreateSessionResponse`, …). Only the surgical RefreshRequest delta was applied. | **P4-T2** (contracts regen) — run full `pnpm contracts:generate` |
| F-3 | P2-T3 | `esbuild.drop` removes `console.error`/`warn` in prod too; no telemetry sink exists. Route prod errors through a logger when one lands. | ~M6 (offline/telemetry hardening) |
| F-4 | P2-T5 | Rate-limit peer-IP keying is correct for the current arch (nginx serves SPA only, no `/api` proxy). If an API-fronting proxy/LB is ever added, switch to a trusted-proxy-count index into XFF. | Deployment hardening (pre-prod) |
| F-5 | P2-T6 | Authenticated `/api/media` `generate_key` still embeds the client filename in the S3 key (F010 targeted the anonymous path). Sanitize when F010's scope widens. | Widen F010 / media hardening |
| F-6 | P2-T7 | Domain verification is fail-closed by default; real DNS-TXT proof needs `--features dns-verify`. Production domain auto-join requires that build flag. | Deployment/build config (note in ops docs) |
| F-7 | live-QA | **Questionnaire-completion progress bar does not render** in the fillout DOM overlay despite `settings.showProgressBar: true` (only the media-loading bar exists). Pre-existing missing feature, not a P1-T2 regression. | New fillout-completeness backlog item (relates to P7-T1 "question N of M" region) |
| F-8 | P4-T1 | The server's `MediaAsset` response has no `width`/`height`/`metadata` (api.ts was reading phantom fields, now typed away). The **reaction/media pillar** needs image dimensions for precise stimulus layout — add them to the media asset schema + server response when that work lands. | Reaction/media epics (E-REACT-*) |
| F-9 | P3-T1 | (RESOLVED, commit 122a3de) 5 pre-existing `clippy -D` lints in src/ blocked CI's clippy step — silenced with `#[allow]`. | — |
| F-10 | E-OFF-1 | Anonymous cross-device server resume falls back to same-device (the `/sessions/{id}/responses` + `/variables` endpoints are auth-gated). Add an anonymous-safe resume-fetch (session-GUC-scoped, like the cohort-stats pattern) so an anonymous participant can resume on another device via the `?sid=` link. | Offline pillar (E-OFF-*) |
| F-11 | E-OFF-1 | The answered-skip set persists for the resumed run, so back-navigation editing of a *pre-resume* answer and loop-replay of a page answered before resume are skipped (forward flow + post-resume loops unaffected). Track pre/post-resume answers separately if back-edit/loop-replay of pre-resume pages is needed. | Flow pillar (E-FLOW-*) |
| F-13 | live-QA | **FIXED (commit 2fbe011):** designer edits lost on quick reload — added 2.5s debounced save-on-edit + `beforeNavigate` + `beforeunload` flush/prompt (30s interval kept as backstop). Verified: edits survive reload on a resolved `/designer/<uuid>` URL. | — |
| F-15 | live-QA | **FIXED (commit 781096d):** the *deeper* root of "designer reverts to blank" — after creating a questionnaire the URL stayed `/designer/new?name=...`, so a reload re-ran create and forked a blank duplicate. Now a `$effect` shallow-`replaceState`s the URL to the real `/designer/<id>` after create. | — |
| F-12b | live-QA | E-FLOW-3 frontend activation DONE (commit bb4b351): `+page` passes `resumeFrom` + WelcomeScreen Continue/Start-over. STILL DEFERRED: Rust cross-device `state_snapshot` (step c) + resume-link token (step d, shared with E-FLOW-2/M8). | E-FLOW-3 completion / M8 |
| F-14 | live-QA (E-OFF-1) | **CRITICAL, FIXED (commit 7b1741b):** fillout froze on Q1 for any modern-shaped question (`inferVariableType` defaulted to `'object'`). Kept here for the ledger. | — |
| F-12 | E-FLOW-3 | **Activate** the tested-but-dormant precise-resume runtime-core: (a) `+page.svelte` load `getResumeState` + pass `resumeFrom` into `FilloutRuntimeConfig`; (b) WelcomeScreen "Continue where you left off" vs "Start over" (step 7); (c) Rust `sessions.rs` update handler accept+return `state_snapshot` + `FilloutUploadSync` ship it for cross-device online resume (step 8); (d) resume-link short-token generator (step 9, shared with E-FLOW-2/M8). Until (a), E-OFF-1's answer-cursor resume is the active path. | E-FLOW-3 completion (before M2 is 100%) |
| F-25 | P8-T10 | `delete_account` sole-member soft-delete uses a member-existence check that ignores `status`, while the 409 owner-guard filters `status='active'`. An org with the departing owner + one *non-active* (invited/pending) member passes the guard (no active others) but is *not* soft-deleted (a member row still exists), so after the membership DELETE it is left active with no owner/active member — a narrow orphan case. No security/crash impact; PII erasure + token revoke are unaffected. Fix: filter the sole-member soft-delete on `status='active'` too, or treat non-active members as absent consistently in both queries. | RBAC/GDPR hardening (M8 E-RBAC-9) |
| F-24 | live-QA (M7-E) | **FIXED (migration 00036 + tests/invitation_schema.rs):** `organization_invitations` (created in 00001) was missing `token`, `status`, `custom_message`, and `declined_at` — every invitation handler (create/list/accept/decline/revoke) referenced them, so the whole feature 500'd at runtime (surfaced as `column i.token does not exist` on the signup email-blur invite check). Additive migration adds the four columns (token UUID default gen_random_uuid() + unique index, status default 'pending', custom_message text, declined_at timestamptz). Verified: exact handler INSERT...RETURNING + list SELECT resolve as qdesigner_app (no RLS block); 2 regression tests green. | — |
| F-26 | live-QA (M7-E) | Intermittent `new row violates row-level security policy for table "organization_members"` in the server log (~20–30s spacing) during an authenticated session; not surfaced in the UI, root cause not isolated (loosely resembles the 30s Yjs presence heartbeat but that channel wasn't always active). Worth isolating — likely a background insert path (presence/analytics) issuing an org_members write without the app.user_id GUC set, or under a policy that denies it. | RBAC/RLS hardening (M8 E-RBAC) |
| F-27 | E-RBAC-6 | **SAML runtime wiring deferred** (OIDC shipped + wiremock-tested). The 00037 schema + admin CRUD accept `protocol='saml'`, but the assertion-consumer handler is not implemented — only OIDC start/callback exist. Wire a samael-based ACS endpoint selected by `protocol` (validate the signed assertion against the IdP cert, map NameID→external_subject, reuse the same JIT-provision + group→role + audit path as OIDC). | E-RBAC-6 completion (post-M8 or when a SAML customer lands) |
| F-28 | E-RBAC-6 | **SSO browser live-QA against a real IdP deferred.** OIDC is covered end-to-end by the wiremock integration test (start→callback→JIT-provision→role-map→JWT), but a full browser run needs a dev IdP (Keycloak in docker-compose). Cheap subset still worth a pass: admin/sso CRUD (secret redaction to has_client_secret, metadata_url reachability reject), and the login 'Sign in with SSO' affordance appearing only for a verified-domain+enabled-OIDC email. | Consolidated M8 live-QA (add Keycloak to docker-compose) |
| F-29 | E-RBAC-7 | **SCIM Group write-provisioning deliberately stubbed (501).** SCIM Users is end-to-end; Groups is a READ reflection of the four org role tiers + members, but POST/PATCH/DELETE on /Groups return SCIM 501 (role assignment stays in the first-class roles API). Wire Group membership PATCH → org-member role changes if an IdP customer requires directory-driven group→role sync. | E-RBAC-7 completion (on customer demand) |
| F-30 | E-RBAC-7 | **API-key rate limiting reuses the shared auth-tuned limiter** (main.rs 10 req/60s, keyed apikey:{id}) — fine for demos, too tight for high-throughput integrations. Add a dedicated, more generous `RateLimiter` instance to AppState for the machine surface rather than widening the auth cap (kept surgical: did not edit state.rs/main.rs/harness). | Deployment/scaling hardening |
| F-31 | E-RBAC-10 | No background scheduler purges expired resource_shares. Functionally inert already (every read predicate + RLS policy filters `expires_at`), and a `purge_expired_resource_shares()` SQL fn exists — but nothing calls it, so rows accrete. Wire it to a periodic job when the server grows scheduler infra (none today). | Housekeeping / scheduler infra |
| F-32 | E-RBAC-10 | A questionnaire-only share grants analytics access (verify_questionnaire_access) but there is no guest-openable analytics-by-questionnaire-id route, so the dashboard renders it as a non-navigating card. Add a read-only guest analytics view keyed by questionnaire id if questionnaire-level sharing should be openable. | Guest UX follow-up |
| F-33 | M8 (consolidated) | **Consolidated M8 browser live-QA pass deferred.** The M8 epics passed type/test/integration gates but several participant/admin surfaces are not yet browser-verified: adaptive-block authoring + CAT fillout (difficulty adaptation, SE-stop, theta on completion), /admin/api-keys (one-time key reveal, scope enforcement, revoke), /admin/data-privacy (export zip download, region immutability, legal-hold, guarded erase), ShareDialog + 'Shared with me' guest flow, SCIM token gen. Run one agent-browser pass over these (SSO login needs a real IdP — see F-28). Green ≠ works (project memory). | End-of-M8 live-QA |
| F-34 | live-QA (M8) | **Data-export in-flight guard has a TOCTOU race.** Sequential re-request correctly 409s ("export already in progress"), but two *truly concurrent* POST /export both succeed (202) and create two jobs — the one-in-flight check isn't atomic. Enforce with a partial unique index on data_exports(organization_id) WHERE status IN ('pending','running') or a SELECT ... FOR UPDATE gate. Minor (double work, not a correctness/security issue). | GDPR hardening |
| F-35 | live-QA (M8) | **FIXED (commit ef3e833) + browser-verified 2026-07-10.** Root cause: BlockManager's template called `ensureAdaptive()` which wrote `$state` during render (Svelte 5 state_unsafe_mutation → scheduler corruption → footer buttons wedge). Config creation moved to dialog-open handlers. ALSO fixed the deeper persistence bug: DocumentStore.commit() normalization omitted 'adaptive' from the type allowlist and dropped adaptive/condition fields, so CAT blocks silently degraded to 'standard' on every save. Live-QA: add→edit→save→reload persists type adaptive; 3× dialog stress responsive. Original entry: Adaptive-block persistence needs a human browser smoke-test. The fix (BlockManager uses designerStore.currentBlockId; addBlock/YjsOps honor type='adaptive' — both code paths verified by inspection) is code-correct, but the re-QA agent could not get a clean signal: agent-browser hit a reproducible Add/Edit-Block dialog freeze (footer buttons wedge after the first interaction) that confounded the check. Have a human add an Adaptive Block in the designer and confirm content persists type:"adaptive" + the AdaptiveBlockEditor (max items/SE/a-b-c) is reachable. | Human designer QA |
| F-36 | live-QA (M8) | **FIXED (fix/yjs-collab-corruption).** ROOT CAUSE confirmed: a classic Yjs multi-seeder anti-pattern, **not** "abnormal automation." Every designer mount ran `questionnaireToYDoc` (CollaborativeDesigner.init), minting fresh CRDT items; those merged (concatenated — Yjs arrays don't dedup by content) into the persistent, never-evicted server room, so the **2nd open** of any questionnaire in one server lifetime (reload, revisit, 2nd collaborator) duplicated every page/block with identical IDs; the client autosaved the dup to `content`. The `authorize_channel` fix (9a0d960) merely made collab reachable, exposing it. **Fix:** server is the sole seeder — new `yjs_state BYTEA` (migration 00043) holds the authoritative CRDT binary; `YjsStore` loads it (or seeds once from `content` under an atomic CAS) on room creation and persists it debounced; clients never seed when online (`init` only seeds offline). Verified by `yjs_seed` round-trip tests + full gate; browser live-QA pending. | Collaboration robustness — FIXED |
| F-37 | fix/yjs-collab-corruption | **Content edited outside collab can leave `yjs_state` stale.** The room now reloads the persisted CRDT binary in preference to `content`. If `content` is mutated out-of-band (bulk import, in-place version edit) while `yjs_state` exists, a collab session shows the older CRDT state and the out-of-band edit is masked until reconciled. Mitigation: invalidate (`SET yjs_state = NULL`) on any non-collab write to `content`, so the next room creation re-seeds. Low risk today (autosave is the only `content` writer for live questionnaires). | Collab/content consistency |
| F-38 | fix/yjs-collab-corruption | **Pre-fix corrupted content is not auto-repaired.** The fix stops NEW duplication but the server now seeds from whatever `content` holds — so the already-corrupted demo questionnaires (649e3e84, e2ea428c, 3cfe3da2) would seed with their existing dupes baked in. Repair = dedupe `content->'pages'` (and nested blocks) by id, or just delete the disposable QA rows. One-shot data fix, not code. | Data cleanup |
| F-39 | R1 live-QA (2026-07-10) | **Canvas keyboard question-delete has no confirm and is the ONLY single-question delete path.** The canvas card / Structure tree expose no delete control for an individual question; selecting it and pressing Delete removes it instantly with no confirmDialog (the button-based deletes gated in R1-7/QuestionCard do confirm). Related to the post-M5 note "canvas inline-editor focus-race — Delete key eats question". Add a visible delete affordance on the question card + route the keyboard path through the shared confirm (or make it undo-friendly). | Designer safety (R4-5 polish) |
| F-40 | R2-2 (2026-07-10) | **Screen-outs are recorded as status='completed' + metadata.screenOut** — the Session status enum has no 'screened'/'ineligible' value, so analytics/exports can't filter screen-outs without parsing metadata JSON. Add a first-class session status (or boolean column) + surface it in exports/analytics when the server schema is next touched. | Server schema / analytics |
| F-41 | R3-1 (2026-07-10) | **Session-read access is org-membership-shaped, twice over.** (a) `verify_questionnaire_access` + the 00021 RLS org-member branch admit ANY active org member to per-session reads — they do not honor `'members'` projectVisibility, so a member excluded from a confidential project can still read its sessions via /api/sessions/{id}/*. (b) Conversely the RLS org-member branch does NOT admit resource_shares grantees, so R3-6's guest analytics cannot ever show per-session detail (its aggregate endpoints are unaffected). Align both when session access is next touched (add a share branch to the *_select_dual policies and/or switch the handler gate to the visibility-aware verify_project_read_access). | RBAC/RLS alignment |
| F-42 | R2/R3 exit QA (2026-07-10) | **Silent IndexedDB failures + growing unsynced backlog while Online.** Console shows 'Auto-save failed: DexieError2' (designer, matches visible toast) and 'Failed to persist resume state: DexieError2' (fillout); the 'N unsaved answers on this device' counter grew 28→84→112 across sessions and never drained despite Online status. Root-cause in progress (diagnosis agent). Possibly quota/DatabaseClosed on the heavily-used shared QA profile, possibly ack-marking not draining. Participant-data risk until explained. | R2 aftercare — DIAGNOSING |
| F-43 | R2/R3 exit QA (2026-07-10) | **Project-list card shows '0 questionnaires' despite the project containing one** (count inside project detail is correct) — list-card aggregate query/cache wrong or stale. | R4 consistency sweep |
| F-44 | R2/R3 exit QA (2026-07-10) | **Runtime features with no designer authoring path:** (a) settings.showProgressBar — no toggle anywhere in the designer settings UI (defaults true, only editable via direct data); (b) consent — ConsentScreen/requireConsent/checkboxes/signature fully supported by the runtime but NOTHING in the designer writes settings.requireConsent or consent content; researchers cannot enable consent for a study through the UI. Add both to the designer settings surface. | R4 designer polish (consent = its own unit) |
| F-47 | R4 exit QA follow-up (2026-07-10) | **No UI entry point opens MediaManager in mode='manage'** (the mode with the per-asset Delete button — zero `mode === 'manage'` call sites in the designer routes/components). Practical effect: researchers cannot delete a media asset from the designer at all. Add a manage-mode entry (e.g. a "Manage library" affordance in the media panel). A leftover 32×16 test-image.png (id f9a7d8d5) sits in the QA org's library because of this. | FIXED (b13e43a) — Manage toggle in the media picker |
| F-52 | F-51 diagnosis (2026-07-10) | **Architectural smell: procedural paradigms eagerly compile a frozen `study.blocks` snapshot that normalize/runtime then prefer over fresh config** — the root soil of F-49/F-51-B/F-51-C (top-level scalars diverge from the stale study copy; three separate "authored field loses to snapshot" bugs so far). Current fixes make editor-owned fields win at every read point, but the durable remedy is to stop compiling `study.blocks` for non-custom paradigms and materialize at publish/run instead. Do this if the class spawns a fourth follow-up. **TRIGGERED (RT-2 re-QA, same day): fourth+fifth instances — designer HYDRATION reads paradigm defaults instead of persisted numerics (then an unrelated save silently clobbers good server data back to defaults), and the published definition still EXECUTES the stale 10-trial study.blocks despite testTrials:4 at top level. Plus reactivity-layer half: 5 ownership_invalid_mutation sites in ReactionTimeDesigner's subtree (:65/:199/:378/:439/:919), Auto-save DataCloneError on every reaction edit (non-cloneable reaching the IDB draft put), TypeError reading feedbackSettings at :293. STRUCTURAL FIX IN FLIGHT.** **FIXED (single-source refactor):** (1) `study` (block plan) is now authored ONLY for the custom paradigm; procedural paradigms carry no `study` — trials materialize from the top-level config at compile/run time. `reaction-normalize` reads scalars/response/stimulus from the top level (`pickNormalizationSource` no longer prefers study-with-blocks) and returns EMPTY blocks for procedural paradigms so the compiler can't execute a stale snapshot; top-level explicit `config.blocks` (reaction-experiment) still always honored. `createDesignerConfig` drops the embedded study for procedural on load (migration) so the next save persists the single-source shape. (2) ownership: ReactionTimeDesigner now OWNS a local `$state` working copy (seeded from the one-way `question` prop) and reflects settled edits through `onUpdate` only — zero `ownership_invalid_mutation`. (3) DataCloneError: the onUpdate payload is a plain `JSON.parse(JSON.stringify(...))` clone (no proxy/closure reaches the IDB draft put). (4) TypeError: `feedbackSettings` seeds from the already-normalized owned config. New/updated tests: compiler F (trial count from top-level testTrials, blocks empty), rune-aware ownership-warning + structured-clone tests, hydration reload tests; check/test/build green. | Reaction model refactor — FIXED |
| F-54 | RT-3 exit QA (2026-07-10) | **Stalled media requests bypass the fail-closed gate — block runs blank stimuli to a 'completed' session.** CDP-aborted image request (never fires load OR error) → the reaction-experiment block proceeded: blank canvas, 4 trials ran to timeout, session completed, trials rows source/rt_us/correct all NULL, zero console warnings. Fast-error loads correctly fail closed via the page-level media screen — but the ReactionEngine canvas gate overlay was never observed in ANY test, so the reaction-lab path's gateBlockMedia engagement is suspect. Fix: (a) hard timeout on Layer-2 preparation (a load that neither resolves nor errors within N seconds = failure), (b) verify/repair gateBlockMedia invocation + stimulus enumeration in the reaction-experiment path, (c) a completed block with zero presented stimuli should be impossible — belt-and-braces invalidation. | FIXED (952f3ce) + overlay 1605c93 |
| F-55 | RT-3 exit QA (2026-07-10) | **Dev-only: clearDevServiceWorkers wipes fillout-media-v2 after every load** (offline.ts:21-41 deletes all fillout-* caches on layout mount unless VITE_SW_DEV=true), racing and clobbering Layer-1 caching so it can't be QA'd in dev. Narrow the wipe to exclude MEDIA_CACHE_NAME or gate harder. | Dev tooling |
| F-58 | RT-6 (2026-07-11) | **Unbounded media fetch blocks the fillout load** — (fillout)/q/[code]/+page.ts awaits cacheQuestionnaire→cacheMedia whose per-asset fetch has NO timeout (cacheServerVariables two lines above is capped at 3s): one hung asset = indefinite blank page, no welcome, no fail-closed UI. Cap the per-asset fetch (Layer-1 is best-effort by design — ADR 0026 — the block-level gate is the guarantee) and let load proceed. | FIXED (5df2dba) + cacheServerVariables cap (5b74ece) |
| F-56 | RT-3 re-verify (2026-07-10) | **Gate overlay may never present during a stalled Layer-2 gate.** Under a genuine stall the STRUCTURAL guarantee held (0 trials, session stays active — F-54 verified), but the canvas showed literally zero drawn pixels (not even the background clear) for 55+s, while canvas text rendering provably works elsewhere in the same session. Hypothesis: the renderer's frame loop is not running while gateBlockMedia holds, so the 'Preparing stimuli…'/retry overlay renderable exists but is never presented — participant sees a blank screen during network trouble. Verify with a held-open media response (test-only route beats QA monkey-patching) and ensure the loop runs (or a frame is explicitly drawn) whenever the gate overlay changes. Also: an abandoned stalled session stays status='active' (orphan; resume machinery covers it — note only). | FIXED (1605c93) — gate paints its own frames |
| F-57 | RT-3 re-verify (2026-07-10) | **Same-version republish may retain stale media/trial references in the pinned offline snapshot** — QA observed 2-3 historical media-asset Image constructions per gate call after republishing without a version bump (ADR 0023 D2 pinning holding onto prior blocks?). Reproduce deliberately: publish → swap a trial's asset → republish same version → fillout should construct ONLY the current asset. | FIXED (5b74ece) — media evicted on republish change; byte-swap-only limit noted |
| F-53 | RT-2 QA pass 3 (2026-07-10) | **Deadletter banner is browser-global and retries deleted questionnaires' sessions indefinitely on unrelated fillouts.** A stale offline session for a DELETED questionnaire keeps its "N answers could not be submitted" banner on EVERY /q/* load (IndexedDB queue not scoped to the current questionnaire) — F-42 stopped the network retry-forever for fully-dead sessions, but the banner/count still surfaces globally. Fix: scope the banner to the current questionnaire's sessions; on server-confirmed-deleted targets surface "this study no longer exists" + offer export/purge instead of a permanent count. | FIXED (5b74ece) — banner scoped per questionnaire + dead-study export/discard |
| F-51 | RT-2 exit QA (2026-07-10) | **Reaction-editor persistence pipeline — 3 coupled bugs (F-49's deeper layer).** (A) The reaction PropertiesPanel mounts ReactionTimeDesigner one-way with a dead onUpdate not in its Props — edits mutate the live $state proxy but never call updateQuestion()/commit(), so isDirty never flips and autosave NEVER fires for anything in the reaction editor (reload-after-wait loses paradigm + all edits; explicit Publish force-saves). (B) Even via Publish, numeric spinbutton values (TimingSpec min/max, Trial Count, Number of Test Trials) revert to paradigm defaults on reload while text/checkbox/select persist — suspect string-typed spinbutton values rejected by normalize → default fallback. (C) correctness never scored at runtime: option_id arrives semantic ("left") but isCorrect stays null on a response matching the persisted correct option — correctOptionIds likely not threaded onto the materialized trials the engine sees. | FIXED (9afb859 + structural 63a597e) |
| F-50 | F-48 diagnosis (2026-07-10) | **create_questionnaire returns 500 instead of 409 on duplicate name** — it uses `?` → `From<sqlx::Error>` → ApiError::Database, bypassing `from_db_error` (error.rs:51) which maps 23505→409. Route the create through from_db_error so clients see a retryable 409. Small server fix; fold into the next server-touching unit. | FIXED (739e9b1) — create maps 23505→409 |
| F-49 | RT-1 exit QA (2026-07-10) | **TimingSpecField edits don't persist (RT-2a defect).** PVT Foreperiod/ISI min/max edits update the UI and survive question-switches but silently revert on reload — the value never reaches the saved content (suspect: reaction-schema/reaction-normalize drops the new `isi` TimingSpec on the save round-trip, or the field writes a local copy outside the update pipeline). Jitter sampling itself verified correct live (distinct per-trial foreperiods) — only authoring persistence is broken. | RT-2a fix — FIXING |
| F-48 | RT-1 exit QA (2026-07-10) | **Designer PERIODIC autosave keeps POSTing create after the first create — every subsequent tick 500s.** 3/3 repro on a clean committed tree (fresh profile/login): initial create succeeds, then the ~20-30s periodic/debounced autosave re-POSTs the create endpoint forever → Postgres `duplicate key ... questionnaire_definitions_project_id_name_version_key` → 500 toast + endless retry, until reload. NOT tied to edit speed — continuous interaction merely defers the doomed tick (why earlier QA passes missed it); any new questionnaire idle for one debounce window hits it. Root shape: the isNew/create-vs-update selection never flips to update-by-id after the successful create on the periodic path (F-13's debounced+interval save / F-15's URL-sync are adjacent but didn't cover this path). May share a root with the old F-26 20-30s-cadence background-write observation. Fix: single-flight the create; once ANY create succeeded, all subsequent ticks (debounced, periodic, retry) must PATCH by id. Verify whether this is a today-regression (contracts/mapper changes) or long-standing. | Designer save path — QUEUED (next after QA pass) |
| F-46 | R4 exit QA (2026-07-10) | **FIXED (fix in flight at close):** MediaManager's empty-state "Upload First Media" CTA called fileInput?.click() but the input only exists inside the showUploadArea block (default false) → dead button on first use. Fix: CTA opens the upload area + triggers the picker. | — |
| F-45 | R4-4 (2026-07-10) | **FIXED (commit 9fece4e) + live-QA verified** (2nd session gated "Study Full" where the stub allowed everyone). Original: Quota conditions silently never gate for compound expressions. `QuotaService.evaluateQuotaCondition` (fillout) is a regex stub understanding only `true/false/1/0` or a single `var <op> value`; anything else — including any `&&`/`||` the designer accepts — returns `true` (allow-by-default), so authored screener/quota conditions don't screen. Fix: evaluate via the scripting-engine FormulaParser/VariableEngine (same parser R4-4's designer validation now uses), fail-closed on parse errors with a logged warning. | Fillout correctness — FIXING |
| F-26↺ | live-QA (M8) | **FIXED (fix/dev-persona-bootstrap).** Root cause fully isolated and it was NOT non-fatal: `/api/dev/bootstrap-personas` seeds on the bare `qdesigner_app` pool with no GUC, and its `INSERT ... ON CONFLICT DO UPDATE` into `organization_members`/`project_members` is RLS-denied — the DO-UPDATE path needs the existing row visible under RLS, which requires a valid `app.user_id` (a plain INSERT or DO NOTHING passes; DO UPDATE does not). It 500s, and because the dev quick-login flow *awaits* this endpoint, dev login was blocked ENTIRELY (personas + org/project never fully seed → login bounces). Fix: seed inside one transaction with `app.user_id` set to the admin persona (also makes it atomic). Verified in-browser: bootstrap-personas → 200, quick-login → /dashboard, designer + all M8 admin surfaces render without auth errors. Side-note surfaced: dev login returns an empty `refresh_token`, so sessions die at the 15-min access-token TTL (no refresh). | Dev tooling — FIXED |
